import React from 'react'
import Link from 'next/link'

interface ButtonInfo {
  fill: boolean;
  color: string;
  fillColor: string;
}

const Button = ({buttonInfo}: {buttonInfo: }) => {
  return (
    <div>Button</div>
  )
}

export default Button